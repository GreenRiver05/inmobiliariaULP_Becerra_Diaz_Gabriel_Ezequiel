@model inmobiliariaBD.Models.Inquilino

@{
    ViewData["Title"] = Model.Id == null ? "Nuevo Inquilino" : "Editar Inquilino";
}
@if (TempData["Mensaje"] != null)
{
    <div class="alert alert-info">@TempData["Mensaje"]</div>
}


<h2>@(Model.Id == null ? "Nuevo Inquilino" : "Editar Inquilino")</h2>

<form asp-action="CreateOrEdit" method="post">
    <input type="hidden" asp-for="Id" />
    @* @if (Model.Id != null)
    {
        <input type="hidden" asp-for="Dni" />
    } *@
    @* @if (ViewBag.PersonaEncontrada != null)
    {
        @if (ViewBag.PersonaEncontrada)
        {
            <div class="alert alert-success">Se encontró al Inquilino. Puede editar sus datos.</div>
        }
        else
        {
            <div class="alert alert-warning">No se encontró un Inquilino con ese DNI. Ingrese los datos para crearlo.</div>
        }

    } *@



    <div class="row">
        <div class="form-group col-md-6 mb-3">
            <label asp-for="Persona.Nombre"></label>
            <input asp-for="Persona.Nombre" class="form-control" />
            <span asp-validation-for="Persona.Nombre" class="text-danger"></span>
        </div>
        <div class="form-group col-md-4 mb-3">
            <label asp-for="Dni"></label>
            <input asp-for="Dni" class="form-control no-spinner" />
            <span asp-validation-for="Dni" class="text-danger"></span>
        </div>
        <div class="form-group col-md-6 mb-3">
            <label asp-for="Persona.Apellido"></label>
            <input asp-for="Persona.Apellido" class="form-control" />
            <span asp-validation-for="Persona.Apellido" class="text-danger"></span>
        </div>
        <div class="form-group col-md-4 mb-3">
            <label asp-for="Persona.Telefono"></label>
            <input asp-for="Persona.Telefono" class="form-control no-spinner" />
            <span asp-validation-for="Persona.Telefono" class="text-danger"></span>
        </div>
        <div class="form-group col-md-6 mb-3">
            <label asp-for="Persona.Direccion"></label>
            <input asp-for="Persona.Direccion" class="form-control" />
            <span asp-validation-for="Persona.Direccion" class="text-danger"></span>
        </div>
        <div class="form-group col-md-4 mb-3">
            <label asp-for="Persona.Correo"></label>
            <input asp-for="Persona.Correo" class="form-control" />
            <span asp-validation-for="Persona.Correo" class="text-danger"></span>
        </div>
        <div class="form-group col-md-6 mb-3">
            <label asp-for="Persona.Localidad"></label>
            <input asp-for="Persona.Localidad" class="form-control" />
            <span asp-validation-for="Persona.Localidad" class="text-danger"></span>
        </div>
        @if (Model.Id != null)
        {
            <div class="form-group col-md-4">
                <div class="d-flex align-items-center gap-2 mt-4">
                    <label class="mb-0">Estado:</label>
                    @if (Model.Estado)
                    {
                        <span class="badge bg-success px-3 py-2">
                            <i class="bi bi-check-circle-fill me-1"></i> Activo
                        </span>
                    }
                    else
                    {
                        <span class="badge bg-secondary px-3 py-2">
                            <i class="bi bi-x-circle-fill me-1"></i> Inactivo
                        </span>
                    }
                </div>
            </div>
        }
    </div>

    <div class="d-flex gap-3 justify-content-center my-4">
        <button type="submit" class="btn btn-primary">Guardar</button>
    </div>


</form>

<div class="d-flex gap-3 justify-content-center my-4">
    @if (Model.Id != null)
    {
        <form asp-action="ModificarEstado" asp-controller="Inquilino" method="post">
            <input type="hidden" name="id" value="@Model.Id" />
            <button type="submit" class="btn btn-sm @(Model.Estado ? "btn-secondary" : "btn-success")">
                @(Model.Estado ? "Dar de baja" : "Dar de alta")
            </button>
        </form>
    }
    @* else{
    <button type="button" class="btn btn-secondary" data-bs-toggle="modal" data-bs-target="#dniModal">
        Buscar por DNI
    </button>
} *@
    <a asp-action="Index" class="btn btn-secondary btn-sm">⬅️ Volver al listado</a>
</div>


<div class="validaciones-info mt-4 mb-0">
    <h5>🔍 Validaciones y/o implementaciones:</h5>
    <ul>
        <li>Verificar formato de correo electrónico ❌</li>
        <li>Controlar longitud del teléfono ❌</li>
        <li>Verificar que no modifica si cambio el dni - Vista editar ❌</li>
        <li>Implementar el modal para buscar por dni ❌</li>
        <li>Sacar spinners de los campos numericos ✅</li>
    </ul>
</div>


@* @section Scripts {
    @if (ViewBag.MostrarModal)
    {
        <script>
            $(document).ready(function () {
                $('#dniModal').modal({ backdrop: 'static', keyboard: false });
                $('#dniModal').modal('show');
            });
        </script>
    }
} *@

<!-- Modal DNI -->
@* <div class="modal fade" id="dniModal" tabindex="-1" role="dialog" aria-labelledby="dniModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <form asp-action="BuscarPorDni" method="post" class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="dniModalLabel">Buscar Persona por DNI</h5>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    <label for="Dni">DNI</label>
                    <input asp-for="Dni" class="form-control" autofocus />
                    <span asp-validation-for="Dni" class="text-danger"></span>
                </div>
            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-primary">Buscar</button>
            </div>
        </form>
    </div>
</div> *@

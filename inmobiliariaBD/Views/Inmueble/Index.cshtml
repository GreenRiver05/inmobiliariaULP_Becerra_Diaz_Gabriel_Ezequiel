@model IEnumerable<inmobiliariaBD.Models.Inmueble>

@{
    ViewData["Title"] = "Listado de Inmuebles";
}

<h2>Listado de Inmuebles</h2>
<a asp-action="CreateOrEdit" class="btn btn-success mb-3">➕ Crear nuevo Inmueble<</a>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Id)</th>
            <th>@Html.DisplayNameFor(model => model.Direccion)</th>
            <th>@Html.DisplayNameFor(model => model.Localidad)</th>
            <th>@Html.DisplayNameFor(model => model.Uso)</th>
            <th>@Html.DisplayNameFor(model => model.Ambientes)</th>
            <th>@Html.DisplayNameFor(model => model.Estado)</th>
            <th>@Html.DisplayNameFor(model => model.Precio)</th>
            <th>@Html.DisplayNameFor(model => model.Propietario)</th>
            <th>@Html.DisplayNameFor(model => model.TipoInmueble.Tipo)</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var inmueble in Model)
        {
            <tr>
                <td>@inmueble.Id</td>
                <td>@inmueble.Direccion</td>
                <td>@inmueble.Localidad</td>
                <td>@inmueble.Uso</td>
                <td>@inmueble.Ambientes</td>
                <td>@inmueble.Estado</td>
                @*Formatea el precio como moneda según la cultura regional (ej: $150.000,50 en es-AR)*@
                <td>@inmueble.Precio.ToString("C")</td>
                <td>@inmueble.Propietario.Persona.ToString</td>
                <td>@inmueble.TipoInmueble.Tipo</td>
                <td>
                    <a asp-action="Details" asp-route-id="@inmueble.Id" class="btn btn-info btn-sm">Ver</a>
                    <a asp-action="Edit" asp-route-id="@inmueble.Id" class="btn btn-warning btn-sm">Editar</a>
                    <a asp-action="Delete" asp-route-id="@inmueble.Id" class="btn btn-danger btn-sm">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
